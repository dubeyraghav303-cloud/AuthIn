{
  "name": "auth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "pnpm -r run generate",
    "db:push": "pnpm --filter @authkit/db db:push",
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}